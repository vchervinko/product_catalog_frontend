import { FC } from 'react';
import { PageLayout } from '../components/PageLayout';

const FavouritesPage: FC = () => (
  <PageLayout title="Favourites" hasControls={false} />
);

export default FavouritesPage;
